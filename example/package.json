{
  "name": "example",
  "version": "1.0.0",
  "description": "Example usage of the Tailwind plugin",
  "scripts": {
    "build": "npx postcss ./input.css -o ./output.css",
    "watch": "npx postcss ./input.css -o ./output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.10",
    "@tailwindcss/postcss": "^4.1.10",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.0"
  }
}
